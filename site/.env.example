MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# Live TTS lives in a separate project (digitaldrama-tts-backend), deployed
# on its own host. Point this at wherever that's running.
PUBLIC_TTS_API_URL=http://localhost:8888/api/tts
